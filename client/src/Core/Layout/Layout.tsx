import React from "react";
import { AppHeader, Main } from "../Helpers";

const Layout = () => {
  return (
    <div>
      {/* <AppHeader /> */}
      <Main />
    </div>
  );
};

export default Layout;
